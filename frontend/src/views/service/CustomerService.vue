<!--
  DEPRECATED in Sprint 41. Kept as an empty stub so old git refs resolve.
  The Sprint 40e iframe embed has been replaced by the AgriOS-native
  conversation workspace (ConversationList.vue + ConversationDetail.vue);
  the /service route now mounts ConversationList directly.

  Safe to delete manually when you next clean up the working tree.
-->
<template>
  <div />
</template>
